# Client logos

Drop 8 client logos here (PNG or SVG, transparent background, ~500px wide, ≤120px tall).
Then in `index.html` replace each `<div class="trust-logo-placeholder">` with:

    <img src="images/clients/<filename>.svg" alt="<Client Name>">

If NDA prevents showing real logos, keep the placeholder text but change to generic descriptors:
"Fortune 500 Telecom Operator", "Hyperscale Data Center", etc.
